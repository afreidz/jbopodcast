---
import Base from "./Layout.astro";
let { class: classList } = Astro.props;
---

<Base class={classList}>
  <slot />
</Base>

<script>
  import client from "$/lib/pocketbase/client";
  import { navigate } from "astro:transitions/client";
  if (!client.authStore.isValid) navigate("/insider/signin");
</script>
